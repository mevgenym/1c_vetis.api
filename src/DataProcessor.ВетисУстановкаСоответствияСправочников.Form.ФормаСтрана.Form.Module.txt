
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Guid") Тогда
		Guid = Параметры.Guid;
	КонецЕсли;
	
	Если Параметры.Свойство("Страна") Тогда
		Страна = Параметры.Страна;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Guid) И НЕ ЗначениеЗаполнено(Страна) Тогда
		Страна = Ветис.Соответствие_ПолучитьСсылку(Guid, ВетисИмяСправочника.Country("Справочник"));
		ЕстьСвязь = ЗначениеЗаполнено(Страна);
	ИначеЕсли НЕ ЗначениеЗаполнено(Guid) И ЗначениеЗаполнено(Страна) Тогда
		Guid = Ветис.Соответствие_ПолучитьGuid(Страна);
		ЕстьСвязь = ЗначениеЗаполнено(Guid);
	Иначе
		ЕстьСвязь = Guid = Ветис.Соответствие_ПолучитьGuid(Страна);
	КонецЕсли;
	
	Элементы.ДекорацияЕстьСвязь1.Видимость = ЕстьСвязь;
	Элементы.ДекорацияЕстьСвязь0.Видимость = НЕ ЕстьСвязь;
	
	КомандаНайтиПоGuidСервер();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
КонецПроцедуры


&НаКлиенте
Процедура КомандаГотово(Команда)
	
	Закрыть(Страна);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаНайти(Команда)
	
	КомандаНайтиПоGuidСервер();
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаДобавить(Команда)
	
	_оо = Новый ОписаниеОповещения("КомандаДобавитьОбработкаОповещения", ЭтаФорма);
	
	_форма = ОткрытьФорму(ВетисИмяСправочника.Country("Справочник", "ФормаОбъекта"),,,,,,_оо);
	
	ЗаполнитьЗначенияСвойств(_форма.Объект, ЭтаФорма);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаДобавитьОбработкаОповещения(пРезультат, пПараметры) Экспорт
	
	Если НЕ пРезультат = Неопределено Тогда
		
		Страна = пРезультат;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДекорацияЕстьСвязь1Нажатие(Элемент)
	
	ВетисВызовСервера.Соответствие_Удалить(Страна);
	
	ЕстьСвязь = Ложь;
	
	Элементы.ДекорацияЕстьСвязь1.Видимость = ЕстьСвязь;
	Элементы.ДекорацияЕстьСвязь0.Видимость = НЕ ЕстьСвязь;
	
КонецПроцедуры

&НаКлиенте
Процедура ДекорацияЕстьСвязь0Нажатие(Элемент)
	
	ВетисВызовСервера.Соответствие_Добавить(Страна, Guid, Наименование);
	
	ЕстьСвязь = Истина;
	
	Элементы.ДекорацияЕстьСвязь1.Видимость = ЕстьСвязь;
	Элементы.ДекорацияЕстьСвязь0.Видимость = НЕ ЕстьСвязь;
	
КонецПроцедуры


&НаКлиенте
Процедура GuidПриИзменении(Элемент)
	
	ЕстьСвязь = Guid = ВетисВызовСервера.Соответствие_ПолучитьGuid(Страна);
	
	Элементы.ДекорацияЕстьСвязь1.Видимость = ЕстьСвязь;
	Элементы.ДекорацияЕстьСвязь0.Видимость = НЕ ЕстьСвязь;
	
КонецПроцедуры

&НаКлиенте
Процедура СтранаПриИзменении(Элемент)
	
	ЕстьСвязь = Guid = ВетисВызовСервера.Соответствие_ПолучитьGuid(Страна);
	
	Элементы.ДекорацияЕстьСвязь1.Видимость = ЕстьСвязь;
	Элементы.ДекорацияЕстьСвязь0.Видимость = НЕ ЕстьСвязь;
	
КонецПроцедуры


&НаСервере
Процедура КомандаНайтиПоGuidСервер()
	
	Если ЗначениеЗаполнено(Guid) Тогда
		
		_Версия20 = Ветис.Версия_2_0();
		
		_country = ?(_Версия20, ВетисIkarService, ВетисIkarService_2_0).GetCountryByGuid(Guid);
		
		Элементы.ГруппаПредупреждение.Видимость = _country = Неопределено;
		Элементы.ГруппаРеквизиты.Видимость = НЕ _country = Неопределено;
		
	КонецЕсли;
	
	Если НЕ _country = Неопределено Тогда
		Наименование = _country.name;
		
	Иначе
		Наименование = "";
	КонецЕсли;
	
КонецПроцедуры
