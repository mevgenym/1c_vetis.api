
&НаКлиенте
Процедура ОсновнойХС(Команда)
	
	ТекщиеДанные = Элементы.Список.ТекущиеДанные;
	
	Если НЕ ТекщиеДанные = Неопределено Тогда
		УстановитьОсновнойХС(ТекщиеДанные.Организация);
		Элементы.Список.Обновить();
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьОсновнойХС(пОрганизация) Экспорт
	
	Константы.ВетисОсновнойХС.Установить(пОрганизация);
	
	ВетисПараметрыСоединения.ИнициализироватьНастройкиХС(пОрганизация);
	
КонецПроцедуры
