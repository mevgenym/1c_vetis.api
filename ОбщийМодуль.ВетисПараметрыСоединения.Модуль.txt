
Функция Адрес() Экспорт
	Возврат "api2.vetrf.ru";
КонецФункции

Функция Логин() Экспорт
	Возврат "";
КонецФункции

Функция Пароль() Экспорт
	Возврат "";
КонецФункции

Функция ЛогинХС() Экспорт
	Возврат "";
КонецФункции

Функция ПарольХС() Экспорт
	Возврат "";
КонецФункции

Функция Порт() Экспорт
	Возврат 8002;
КонецФункции

Функция serviceId() Экспорт
	Возврат "mercury-g2b.service";
КонецФункции

Функция issuerId() Экспорт
	Возврат "";
КонецФункции

Функция apiKey() Экспорт
	Возврат "";
КонецФункции

Функция enterpriseGuid(index = 0) Экспорт
	
	хс = ВетисEnterpriseService.GetBusinessEntityByGuid(issuerId());
	
	Если index < хс.activityLocation.Количество() Тогда
		Возврат хс.activityLocation[index].enterprise.guid;
	Иначе
		Возврат issuerId();
	КонецЕсли;
	//Для каждого пр Из хс.activityLocation Цикл
	//	Возврат пр.enterprise.guid;
	//КонецЦикла;
	
	//http://vetrf.ru/vetrf-forum/posts/list/150/6855.page#39303
	//Вы указали issuerID, а он идентифицирует хозяйствующий субъект (businessEntity).
	// Хозяйствующий субъект - это юридическое лицо, имеющее реквизиты.
	// enterpriseGUID - это идентификатор предприятия, площадки, на которой хозяйствующий субъект осуществляет свою деятельность.
	// Площадка должна быть связана с хозяйствующим субъектом. 
	//Если у вас на тестовом сервере были оформлены записи журнала, то алгоритм для вас может быть следующим: 
	//1. Выполнить метод getBusinessEntityByGUID, указав в качестве GUID ваш issuerId.
	//   В результате вам будет выдана информация о вашем хозяйствующем субъекте.
	//   В объекте activityLocations будут содержаться guid'ы привязанных к вашему ХС предприятий. 
	//2. Если привязанные предприятия есть, то вы берёте один из GUID'ов и подставляете в enterpriseGUID нужного вам запроса. 
	//3. Если предприятий не привязано, то их можно найти по названию с помощью метода getRussianEnterpriseList
	//   и привязать к своему ХС с помощью метода modifyActivityLocations. 
	//4. Если ваши предприятия не внесены в реестр вообще, то их можно занести самостоятельно с помощью метода modifyEnterprise
КонецФункции
