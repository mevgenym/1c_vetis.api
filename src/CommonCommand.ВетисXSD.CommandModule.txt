
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	_оо = Новый ОписаниеОповещения("ТекстОбработкаОповещения", ЭтотОбъект);
	
	_строка = "";
	
	Для каждого _строках Из ВетисXSD.XSDПолучитьURL() Цикл
		_строка = _строка + Символы.ПС + _строках;
	КонецЦикла;
	
	Для каждого _строках Из ВетисXSD.WSDLПолучитьURL() Цикл
		_строка = _строка + Символы.ПС + _строках;
	КонецЦикла;
	
	ПоказатьВводСтроки(_оо, _строка,,, Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ТекстОбработкаОповещения(Результат, Параметр) Экспорт
	
КонецПроцедуры
