
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Версия = Константы.ВетисВерсия.Получить();
	
	_НастройкиХС = РегистрыСведений.ВетисНастройкиХС.СоздатьНаборЗаписей();
	_НастройкиХС.Прочитать();
	СписокНастройкиХС.Загрузить(_НастройкиХС);
	
	_Пользователи = РегистрыСведений.ВетисПользователи.СоздатьНаборЗаписей();
	_Пользователи.Прочитать();
	СписокПользователи.Загрузить(_Пользователи);
	
КонецПроцедуры

&НаСервере
Процедура КомандаГотовоНаСервере()
	
	Константы.ВетисВерсия.Установить(Версия);
	
	ВетисОбщегоНазначения.ВерсияУстановить(Версия);
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаГотово(Команда)
	
	КомандаГотовоНаСервере();
	
КонецПроцедуры
