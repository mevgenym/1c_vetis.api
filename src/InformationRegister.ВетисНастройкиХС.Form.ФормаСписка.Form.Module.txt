
&НаКлиенте
Процедура ОсновнойХС(Команда)
	
	ТекщиеДанные = Элементы.Список.ТекущиеДанные;
	
	Если НЕ ТекщиеДанные = Неопределено Тогда
		УстановитьОсновнойХС(ТекщиеДанные.ХозяйствующийСубъект);
		Элементы.Список.Обновить();
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьОсновнойХС(пХС) Экспорт
	
	Константы.ВетисОсновнойХС.Установить(пХС);
	
	ВетисПараметрыСоединения.ИнициализироватьНастройкиХС(пХС);
	
КонецПроцедуры
