
Функция GetBusinessEntityByGuid(guid) Экспорт
	
	Возврат Прокси().GetBusinessEntityByGuid(guid);
	
КонецФункции

Функция GetBusinessEntityList(пФабрика, inn = Неопределено) Экспорт
	
	отборХС = ВетисCerberusEnterprise.BusinessEntity(пФабрика, , inn);
	
	Возврат Прокси().GetBusinessEntityList(ВетисBase.ListOptions(пФабрика), отборХС);
	
КонецФункции

Функция GetEnterpriseByGuid(guid) Экспорт
	
	Возврат Прокси().GetEnterpriseByGuid(guid);
	
КонецФункции

Функция Прокси()
	
	_Прокси = WSСсылки.EnterpriseServiceTest.СоздатьWSПрокси("http://api.vetrf.ru/schema/cdm/registry/service", "EnterpriseServiceBindingQSService", "EnterpriseServiceBindingQSPort");
	_Прокси.Пароль       = ВетисПараметрыСоединения.Пароль();
	_Прокси.Пользователь = ВетисПараметрыСоединения.Логин();
	
	Возврат _Прокси;
	
КонецФункции
