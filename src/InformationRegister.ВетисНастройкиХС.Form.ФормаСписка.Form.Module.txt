
&НаКлиенте
Процедура ОсновнойХС(Команда)
	
	ТекщиеДанные = Элементы.Список.ТекущиеДанные;
	
	Если ТекщиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВетисВызовСервера.ОсновнойХС(ТекщиеДанные.ХозяйствующийСубъект);
	
	ВетисВызовСервера.ИнициализироватьНастройкиХС();
	
	Элементы.Список.Обновить();
	
КонецПроцедуры
