
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	#Если МобильноеПриложениеСервер Тогда
		Элементы.Группа1.Группировка = ГруппировкаКолонок.Вертикальная;
		Элементы.Расшифровка.АвтоВысотаЯчейки = Истина;
		Элементы.Миллисекунды.Видимость = Ложь;
		Элементы.Группа4.Видимость = Ложь;
	#Иначе
		Элементы.ВерсияМП.Видимость = Ложь;
	#КонецЕсли
	
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьЖурнал(Команда)
	
	Если Вопрос("Уверен?", РежимДиалогаВопрос.ДаНет,, КодВозвратаДиалога.Нет, "Очистить Журнал") = КодВозвратаДиалога.Да Тогда
		
		ВетисВызовСервера.Журнал_Очистить();
		
		Элементы.Список.Обновить();
		
	КонецЕсли;
	
КонецПроцедуры
