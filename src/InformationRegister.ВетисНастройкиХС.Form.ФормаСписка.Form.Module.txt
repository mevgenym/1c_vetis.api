
&НаКлиенте
Процедура ОсновнойХС(Команда)
	
	ТекщиеДанные = Элементы.Список.ТекущиеДанные;
	
	Если ТекщиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВетисПараметрыСоединения.ОсновнойХС(ТекщиеДанные.ХозяйствующийСубъект);
	
	ВетисПараметрыСоединения.ИнициализироватьНастройкиХС();
	
	Элементы.Список.Обновить();
	
КонецПроцедуры
